<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only cleartext allowance. The local backend runs plain HTTP; the emulator
  reaches the host Mac's loopback via the special alias 10.0.2.2. Cleartext is
  permitted ONLY to these dev hosts — everything else stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
