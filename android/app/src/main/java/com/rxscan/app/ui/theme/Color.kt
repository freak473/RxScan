package com.rxscan.app.ui.theme

import androidx.compose.ui.graphics.Color

// Design tokens lifted directly from RxScan-v2-design-v3.html.
// Paper / ink / green language: paper-pad backgrounds, ballpoint-blue for anything
// handwritten (extracted values), brand green for chrome, amber ONLY for re-check flags.

val Green950 = Color(0xFF07352C)
val Green900 = Color(0xFF0A4A3E)
val Green = Color(0xFF0E6E5B) // brand
val GreenSoft = Color(0xFFE7F0EB)
val GreenTint = Color(0xFFF0F5F1)

val Paper = Color(0xFFFBF8F1) // prescription-pad white
val PaperDeep = Color(0xFFF4EFE3)
val PaperLine = Color(0xFFE9E1CF)
val ChipPaper = Color(0xFFFFFDF4)

val Ink = Color(0xFF1E3D8F) // ballpoint blue — anything handwritten/extracted
val InkFaint = Color(0xFF8A98C4)

val TextPrimary = Color(0xFF17211C)
val Muted = Color(0xFF5C685F)
val Faint = Color(0xFF96A29A)

val Amber = Color(0xFFA66A08) // re-check flags ONLY
val AmberBg = Color(0xFFFBF3DD)
val AmberLine = Color(0xFFEAD9A8)

val RxRed = Color(0xFFB0503F)
val RxRedLine = Color(0xFFE7CFC7)

val White = Color(0xFFFFFFFF)
